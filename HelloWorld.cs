using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update privet pupsik
    void Start()
    {
        //vot tak mosgno vivodit simvoli
        print("Hello, World!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
